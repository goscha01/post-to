{
  "name": "post-to-backend",
  "version": "1.0.0",
  "description": "Backend API server",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "postinstall": "node -e \"if(process.env.RAILWAY_ENVIRONMENT==='prod'){try{require('child_process').execSync('npx --yes @fixprompt/cli deploy-start --source post-to-prod --key \\\"'+process.env.FIXPROMPT_KEY+'\\\" --status success',{stdio:'inherit'})}catch(e){console.warn('[fixprompt] deploy marker failed:',e.message)}}\""
  },
  "dependencies": {
    "@fixprompt/node": "^0.0.2"
  }
}